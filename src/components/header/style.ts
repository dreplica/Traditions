import styled from 'styled-components';

export const Container = styled.div`
         position: sticky;
         padding-top:20px;
         background: rgb(24, 17, 17);
         top: 0;
         z-index:10;
       `;
